import 'package:flutter/material.dart';

class Kaleb extends StatelessWidget {
  const Kaleb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Kaleb')));
  }
}
